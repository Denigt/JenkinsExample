pipeline {
// Programa que puede ejecutar la Pipeline (Windows, Docker o cualquier otro programa capaz de ejecutar Jenkins)
    agent any
    
    stages {
    // Etapa/Paso de Pipeline
        stage('Hello') {
        // Pasos a seguir en esta etapa
            steps {
                echo 'Hello World'
            }
        }
    }
}
